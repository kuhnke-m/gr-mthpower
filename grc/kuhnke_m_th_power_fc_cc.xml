<?xml version="1.0"?>
<block>
  <name>M-th-power frequency correction</name>
  <key>kuhnke_m_th_power_fc_cc</key>
  <category>[kuhnke]</category>
  <import>import kuhnke</import>
  <make>kuhnke.m_th_power_fc_cc($M, $N, $symbol_rate)</make>
  <!-- Make one 'param' node for every Parameter you want settable from the GUI.
       Sub-nodes:
       * name
       * key (makes the value accessible as $keyname, e.g. in the make node)
       * type -->
  <param>
    <name>M (as in M-PSK)</name>
    <key>M</key>
    <type>int</type>
  </param>
  <param>
    <name>Symbol Rate (=Sample Rate in this case)</name>
    <key>symbol_rate</key>
    <type>float</type>
  </param>
  <param>
    <name>N (average frequency estimate)</name>
    <key>N</key>
    <type>int</type>
  </param>

  <!-- Make one 'sink' node per input. Sub-nodes:
       * name (an identifier for the GUI)
       * type
       * vlen
       * optional (set to 1 for optional inputs) -->
  <sink>
    <name>in</name>
    <type>complex</type>
  </sink>

  <!-- Make one 'source' node per output. Sub-nodes:
       * name (an identifier for the GUI)
       * type
       * vlen
       * optional (set to 1 for optional inputs) -->
  <source>
    <name>out</name>
    <type>complex</type>
  </source>
  <source>
    <name>f_est</name>
    <type>float</type>
  </source>
</block>
